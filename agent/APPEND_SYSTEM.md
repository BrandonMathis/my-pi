Prefer Ruby over Python when viable
When running code inline through shell commands, prefer a readable multi-line string form with proper indentation over a dense one-liner whenever the shell supports it.
